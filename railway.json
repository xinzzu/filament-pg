{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan config:clear && php artisan route:clear && php artisan view:clear && php artisan storage:link && chmod -R 775 storage bootstrap/cache"
  },
  "deploy": {
    "startCommand": "sleep 30 && php -r 'echo \"Testing MySQL with DATABASE_URL...\\n\"; try{$pdo=new PDO(\"mysql://root:wZdrHVBeYDbdDnVgcckddvGkuDFHHUKL@mysql.railway.internal:3306/railway\");echo \"DATABASE_URL connection: SUCCESS!\\n\";}catch(Exception $e){echo \"DATABASE_URL failed: \".$e->getMessage().\"\\n\";}' && php artisan migrate --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=$PORT",
    "runtime": "V2",
    "numReplicas": 1,
    "sleepApplication": false,
    "multiRegionConfig": {
      "asia-southeast1-eqsg3a": {
        "numReplicas": 1
      }
    },
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}